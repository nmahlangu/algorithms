given a number, return the next highest number lexigraphically (e.g. 1432 -> 2134)

return all permutations of a number w/o recursion (use bitshifting)

given words sorted by some alphabetic ordering, return a valid ordering of all the letters in that alphabet

design a url shortner

how would you design a website to handle donations?

given a list of decimal prices [p1,...] create new list of integer prices via floor/ceiling [q1,...] such that rounded(sum(p)) = sum(q)